#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the programme
 * @argc: contrains the number of arguments
 * @argv: contains the number oarray of arguments
 * Return: Always return 0 for success
 */

int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", *argv);
return (0);
}
